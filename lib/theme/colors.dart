import "package:flutter/material.dart";

class AppColors {
  // Brand — warm amber/brown, matches AppTextStyles.heading1.
  static const Color primary = Color(0xFFAB6B11);
  static const Color primaryLight = Color(0xFFD99C3F);
  static const Color primaryDark = Color(0xFF7A4B0A);

  // Secondary accent — deep leather brown, for contrast against the amber.
  static const Color secondary = Color(0xFF5C4033);
  static const Color secondaryLight = Color(0xFF8A6B5C);

  // Neutrals — warm off-white paper tones instead of stark white/grey.
  static const Color background = Color(0xFFFDF8F0);
  static const Color surface = Color(0xFFFFFCF5);
  static const Color onSurfaceMuted = Color(0xFF6F6459);
  static const Color shadow = Color(0x1A2B2118);

  static const Color textPrimary = Color(0xFF2B2118);
  static const Color textSecondary = Color(0xFF5C4033);

  static const Color error = Color(0xFFB3261E);

  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color(0xFFFFFFFF);
}
